package com.example.newsapp.ui.overview

import androidx.lifecycle.LiveData
import androidx.lifecycle.MutableLiveData
import androidx.lifecycle.ViewModel
import androidx.lifecycle.viewModelScope
import androidx.paging.Pager
import androidx.paging.PagingConfig
import androidx.paging.PagingData

class OverviewViewModel: ViewModel() {



//    private val _navigateToSelectedProperty = MutableLiveData<Articles?>()
//    val navigateToSelectedProperty: LiveData<Articles?>
//        get() = _navigateToSelectedProperty

    //Pager
//    val flow: LiveData<PagingData<Articles>> = Pager(
//        PagingConfig(pageSize = 5)
//    ) {
//
//        EverythingNewsPagingSource(NewsApi.retrofitService, "sport", language.value!!)
//    }.liveData.cachedIn(viewModelScope)

}